default: &default
  pool: 5
  adapter: postgresql
  encoding: unicode

development:
  <<: *default
  database: hash_development

test:
  <<: *default
  database: hash_test

# Heroku
production:
  adapter: postgresql
  encoding: unicode
  host: localhost
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  database: DATABASE
  username: user
  password: <%= ENV['MYAPP_DATABASE_PASSWORD'] %>
